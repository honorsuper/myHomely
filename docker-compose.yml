version: "3.8"

services:
  nest-app:
    build:
      context: ./
      dockerfile: ./Dockerfile
    ports:
      - '443:443' 
      - '80:80'
    volumes:
      - /Users/honorsuper/nginx/cert:/etc/nginx/cert


